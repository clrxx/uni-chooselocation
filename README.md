# 项目使用方法（必看）
```
只支持App端。
下载后请依次解压项目。
本项目使用的cli模式创建并开发的。
如果你使用的是cli模式开发，请在解压后的 uni-chooselocation 文件夹里执行 npm install，然后用 HBuilder X 打开并执行启动。
如果你使用的是可视化窗口创建的项目，请把 src 文件夹复制出来，更改名字，然后用 HBuilder X 打开并执行启动。
对应使用的key请自行前往相关地图开放平台进行申请。
```
